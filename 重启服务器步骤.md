# 🔧 重启 Nuxt 服务器步骤

## ⚠️ 错误信息
```
Create a Vue component in the pages/ directory to enable <NuxtPage>
```

## ✅ 解决方案

### 步骤 1: 停止当前服务器

在终端中按 **`Ctrl + C`** 停止正在运行的开发服务器。

### 步骤 2: 清除 Nuxt 缓存（推荐）

```powershell
# 删除 .nuxt 目录
Remove-Item -Recurse -Force .nuxt -ErrorAction SilentlyContinue

# 删除 .output 目录
Remove-Item -Recurse -Force .output -ErrorAction SilentlyContinue
```

或者使用简单命令：
```bash
npm run clean
```

如果没有 clean 命令，手动删除：
- 删除 `.nuxt` 文件夹
- 删除 `.output` 文件夹

### 步骤 3: 重新启动开发服务器

```bash
npm run dev
```

### 步骤 4: 等待编译完成

你会看到类似的输出：
```
✔ Vite client built in XXXms
✔ Vite server built in XXXms

  ➜ Local:   http://localhost:3000/
  ➜ Network: use --host to expose
```

### 步骤 5: 访问页面

打开浏览器访问：**http://localhost:3000/**

---

## 🎯 验证文件是否存在

运行以下命令确认文件存在：

```powershell
# 查看 pages 目录内容
Get-ChildItem pages
```

应该看到：
- index.vue
- static.vue
- realtime.vue
- test.vue

---

## 🐛 如果仍然有问题

### 方案 1: 检查 pages 目录位置

确保 `pages/` 目录在项目根目录，而不是在其他地方：

```
okx_trading/          ← 项目根目录
├── app/
├── pages/            ← 应该在这里
│   ├── index.vue
│   ├── static.vue
│   └── realtime.vue
├── components/
└── package.json
```

### 方案 2: 重新安装依赖

```bash
# 删除 node_modules 和锁文件
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json

# 重新安装
npm install
```

### 方案 3: 检查 Nuxt 配置

查看 `nuxt.config.ts`，确保没有禁用路由：

```typescript
export default defineNuxtConfig({
  compatibilityDate: '2025-07-15',
  devtools: { enabled: true }
  // 确保没有 pages: false 或其他禁用路由的配置
})
```

---

## ✨ 成功标志

启动成功后，访问 http://localhost:3000/，你应该看到：

1. **首页**: 显示"📊 K线图系统"标题
2. **两个卡片**: 静态数据版本 和 实时数据版本
3. **无错误**: 控制台没有 "Create a Vue component" 错误

---

## 📞 快速命令参考

```powershell
# 一键重启（复制整段运行）
cd c:\Users\night\Desktop\okx_trading
Remove-Item -Recurse -Force .nuxt -ErrorAction SilentlyContinue
npm run dev
```

